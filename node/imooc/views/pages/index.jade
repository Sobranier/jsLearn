extends ../layout

block content
    .container
        .row
            each item in movies
                .col-md-2
                    .thumbanil
                        a(href="/movies/#{item._id}")
                            img(src="#{item.poster}", alt="#{item.title}")
                            h3 #{title}
                            p: a.btn.btn-paimary(href=""/movie/#{item._id}, role="button")
                                观看预告片
